# Replace <your-docker-registry> with your Docker Hub username or private registry URL
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: model
  name: model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: model
    spec:
      containers:
      - image: arashmsn/house-price-pred-model:latest
        name: house-price-pred-model
        ports:
        - containerPort: 8000
        resources: {}
status: {}
